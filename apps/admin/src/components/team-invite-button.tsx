"use client";
import {useState} from "react";
import {X} from "lucide-react";
const roles=[
  ["super_admin","Super admin"],["admin","Administrator"],["publisher","Publisher"],["content_editor","Content editor"],["marketing_manager","Marketing manager"],["virtual_assistant","Virtual assistant"],["analyst","Analyst"],["support_agent","Support agent"],["auditor","Auditor"],
] as const;
export function TeamInviteButton(){const [open,setOpen]=useState(false),[email,setEmail]=useState(""),[role,setRole]=useState("virtual_assistant"),[pending,setPending]=useState(false),[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setPending(true);setMessage("");try{const r=await fetch("/api/team/invite",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,roleKey:role})}),b=await r.json();if(!r.ok)throw new Error(b.error);setMessage("Invitation created with the selected role.");setEmail("")}catch(e){setMessage(e instanceof Error?e.message:"Unable to create invitation.")}finally{setPending(false)}}return <>{<button type="button" onClick={()=>setOpen(true)} className="relative shrink-0 rounded-2xl bg-primary px-5 py-2.5 text-xs font-bold text-white shadow-[0_4px_14px_rgba(9,95,199,0.28)] transition hover:bg-primary-hover active:scale-95">Invite member</button>}{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/30 p-4" onClick={()=>setOpen(false)}><section role="dialog" aria-modal="true" aria-labelledby="invite-title" className="w-full max-w-md rounded-3xl border border-border bg-white p-6 shadow-2xl" onClick={e=>e.stopPropagation()}><div className="flex items-start justify-between"><div><h2 id="invite-title" className="text-lg font-bold text-navy">Invite a team member</h2><p className="mt-1 text-xs text-muted">Choose the least-privilege role they need.</p></div><button type="button" aria-label="Close invite dialog" onClick={()=>setOpen(false)} className="rounded-full p-2 text-muted hover:bg-surface-subtle"><X size={17}/></button></div><form className="mt-6 space-y-4" onSubmit={submit}><label className="block text-xs font-semibold text-slate-700">Work email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-border px-3 text-sm outline-none focus:border-primary" placeholder="name@company.com"/></label><label className="block text-xs font-semibold text-slate-700">Invite as<select value={role} onChange={e=>setRole(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-border bg-white px-3 text-sm outline-none focus:border-primary">{roles.map(([value,label])=><option value={value} key={value}>{label}</option>)}</select></label>{message&&<p role="status" className="rounded-xl bg-surface-subtle p-3 text-xs text-navy">{message}</p>}<button disabled={pending} className="h-11 w-full rounded-xl bg-primary text-sm font-semibold text-white hover:bg-primary-hover disabled:opacity-60">{pending?"Creating invitation…":"Create invitation"}</button></form></section></div>}</>}
